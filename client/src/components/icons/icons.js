import React from 'react';
import { Home, Card, Users, Settings, Invoice, DownArrow, Close } from './index';

const iconList = {
  home: <Home />,
  card: <Card />,
  users: <Users />,
  settings: <Settings />,
  invoice: <Invoice />,
  downArrow: <DownArrow />,
  close: <Close />,
};

export { iconList };
